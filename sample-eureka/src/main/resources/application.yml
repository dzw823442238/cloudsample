
server:
  port: 8081
eureka:
  instance:
    hostname: localhsot
  client:
    register-with-eureka: false
  #  fetch-registry: false
    service-url:
      defaultZone: http://192.168.1.4:${server.port}/eureka
spring:
  application:
    name: sample-eureka
